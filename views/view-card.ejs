<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/viewCard.css">
    <link rel="stylesheet" href="/css/addcard.css">
    <title>Document</title>
</head>
<body>
    
    <div class="navigation">
        <a class="back-to-dashboard" href="/Dashboard"><h1>บอร์ด</h1></a>
        <p class="card-title"><h2>/ <%= card.title %></h2></p>

        <form action="/dashboard/item-delete/<%= cardID %>?_method=delete" method="POST">
            <button class="dl-btn" type="submit">ลบ</button>
        </form>
    </div>

    <div class="task-form-box">
        <form action="/dashboard/item/<%= cardID %>?_method=put" method="post" class="task-form">
            <!-- <div class="add-task-title">เพิ่มงาน</div> -->

            <input type="text" placeholder="ชื่องาน" value="<%= card.title %>" name="title" id="title">
            <textarea class="detail-task" placeholder="รายละเอียดงาน" name="body" id="body" cols="30" rows="10"><%= card.body %></textarea>
            <input type="text" placeholder="ผู้รับผิดชอบ" value="<%= card.responsiblePerson %>" name="responsiblePerson" id="responsiblePerson">
            <input type="date" placeholder="กำหนดส่ง" value="<%= card.deadline %>" name="deadline" id="deadline">
            <input type="text" placeholder="สถานะ" value="<%= card.workstatus %>" name="workstatus" id="workstatus">
            <button type="submit">อัพเดต</button>
        </form>
    </div>
</body>
</html>